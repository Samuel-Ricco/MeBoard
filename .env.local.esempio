# Copia questo file in `.env.local` e riempilo dal pannello Supabase:
#   Project Settings -> API
#
# `.env.local` NON entra nel repo. La anon key e' pubblica per disegno --
# finisce nel bundle e a proteggere i dati e' RLS -- ma legarla ai commit
# renderebbe impossibile ruotarla senza riscrivere la storia.

VITE_SUPABASE_URL=https://srmjqlhprumjvocspbgj.supabase.co
VITE_SUPABASE_ANON_KEY=
